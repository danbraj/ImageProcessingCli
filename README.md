# ImageProcessingCli

## Installation

1. Install .NET Core
1. Clone repository
1. Restore project (`dotnet restore`)
1. Build project (`dotnet build`)
1. Run application (`dotnet run`)

## Usage

todo